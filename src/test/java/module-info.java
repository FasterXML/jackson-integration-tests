// Module-info for integration tests, actual tests
module tools.jackson.integtest.test
{
    // Same deps as "non-test" side (lovely duplication)
    requires tools.jackson.integtest;

    // But for Kotlin seem to need some of the same deps
}
